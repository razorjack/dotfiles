# Shared environment for macOS and Linux, sourced on the env path by both zsh
# and bash. Env only - aliases, functions and prompt colours live in
# interactive.sh. Keep it bash-compatible: no zsh-only syntax.

export EDITOR='nvim'

export LC_ALL=en_US.UTF-8
export LANG=en_US.UTF-8

export DIRENV_LOG_FORMAT=''
