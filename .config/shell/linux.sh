# Linux-specific environment. Sourced from ~/.profile after common.sh. Env only;
# interactive bits (ls colours) live in interactive.sh.

# Where .zshrc finds zsh-syntax-highlighting / zsh-completions (no Homebrew on
# Fedora; both packages install under /usr/share).
export RZR_PREFIX=/usr
